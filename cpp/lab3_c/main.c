#include <stdio.h>
#include <stdlib.h>
#include "lab3_c.h"

int main()
{
    // printReversedNumber();

    // int rowLength = 0;
    // int nrRows    = 0;
    // int* first    = 0;
    // int* second   = 0;
    // readIntoMatrices(&first, &second, &nrRows, &rowLength);
    // calculateSumAndDisplay(first, second, nrRows, rowLength);

    // printSumS1();

    printUpdatedVector();
}